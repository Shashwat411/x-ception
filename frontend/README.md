# Frontend

React-based UI for administrative dashboard and call interface.

## Getting Started

```bash
cd frontend
npm install
npm start
```

Components will handle real-time updates from backend via WebSocket or REST.
